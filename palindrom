class Solution {
    public boolean isPalindrome(int x) {
        if(x<0)
            return false;
            int y=x;
    int rev=0,r;
//what will be logic here??
            }
            if(rev==x)
            return true;
            else
                return false;
        }
    }


